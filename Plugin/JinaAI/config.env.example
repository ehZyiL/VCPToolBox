# JinaAI Plugin 配置示例
# ========================
# 
# 使用说明：
# 1. 复制此文件为 config.env
# 2. 从 https://jina.ai/?sui=apikey 获取你的免费 API 密钥
# 3. 将下面的 YOUR_API_KEY_HERE 替换为你的实际 API 密钥
# 4. 保存文件

# Jina AI API 密钥（必需）
JINA_API_KEY=YOUR_API_KEY_HERE

# Reader API Token 使用设置（可选，默认为 false）
# Reader API 不强制使用 token，但使用 token 后获取效率会更高
# 设置为 true 启用 token 认证，false 为不使用 token
JINA_READER_USE_TOKEN=false

# 调试模式（可选，默认为 false）
# DebugMode=true